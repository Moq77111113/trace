// If you see this file, you have not run the auth:schema script yet.
// M6.1 runs `pnpm auth:schema` to generate the Better Auth tables here.
// Until then, this file is intentionally empty (typed as a module via `export {}`).

export {};
