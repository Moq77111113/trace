# Drizzle / Postgres (matches docker-compose.yml)
DATABASE_URL="postgres://trace:trace@localhost:5432/trace"

# App
ORIGIN=""

# Better Auth — for production use 32+ random characters
# https://www.better-auth.com/docs/installation
BETTER_AUTH_SECRET=""

# S3 / MinIO (matches docker-compose.yml)
S3_ENDPOINT="http://localhost:9000"
S3_REGION="us-east-1"
S3_BUCKET="trace-attachments"
S3_ACCESS_KEY="trace"
S3_SECRET_KEY="tracetrace"
